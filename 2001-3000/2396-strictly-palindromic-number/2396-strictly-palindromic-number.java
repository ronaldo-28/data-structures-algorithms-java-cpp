class Solution {
    public boolean isStrictlyPalindromic(int n) {
        if (n < 4) {
            return true; // Technically, the problem states n >= 4, but for completeness
        }
        return false;
    }
}